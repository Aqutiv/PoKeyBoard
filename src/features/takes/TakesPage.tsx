/** Takes library skeleton — list and actions land with persistence. */
export function TakesPage() {
  return (
    <section className="page" aria-label="Takes">
      <header className="page__header">
        <h1 className="page__title">Takes</h1>
      </header>
      <p className="page__hint">Your saved takes will appear here.</p>
    </section>
  );
}
